循环代码提部分:
    1,在循环开始之后,  判断是否满足执行循环条件,不满足跳出当前循环或总循环
        1.1, 通过i-- 的方式重新执行当前的循环
        1.2, 通过
    2,进行循环操作 使用continue + return进行多态
        2.1 判断条件执行的内容
        2.2 执行固定的内容
    3,在下载循环开始之前, 判断是否满足终止循环条件 使用break 进行跳出
    简称 if 普通 for 普通 if
        for当中又式if for if

代码体流程 
    记录当前数据  + 展示当前数据  主要是用于当前的程序
    记录当前状态 + 展示当前状态 主要用于调试当前的程序
    改变当前数据
    改变当前状态

    状态传递

模板总结:
    嵌套:每一个循环内部就是一个新的代码体
    思路:每一个循环都有自己的目的,使用特殊的语句进行多态循环的编写

流程总结：！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！
    0，定义容器
    1，固定改变：初始化
        初始化是为了形成比较标准，往往作为初始化进行比较
        在循环开始位置的改变 为了这个循环

    2，条件改变
        1，改变 
        2，记录，打印，查看 ： 
            记录当前运行坐标位置
            记录当前运行的结果

    3，固定改变：为下个循环做下准备
            切换当前运行的坐标break
            切换运行方向break
    eg：
        循环模板2 emmmmmmmmmmmmx
        
    