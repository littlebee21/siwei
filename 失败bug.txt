失败bug
    笔误问题：
        不能被编译器发现的编写错误：
            = 和 == 混淆
            ！ 和 1 混淆
            0 和 o 混淆
            & 和 && 混淆
            整数溢出问题

        缺少i++ 环节
        缺少*号
        初始化的语句不正确
        访问指针的区域溢出，访问区域越界
            数组的下标
            地址的偏移量
        使用指针作为缓存   
        标点符号写错： 未定义
        拼写错误：未定义
        malloc的内存分配的错误：
            malloc乱码
    思路不清问题：
        解决方案：完全按照模板进行编写：
            初始化
            执行
            记录
            改变
        缺少相关元素的初始化内容： emmmmmmmx
            解决方案：初始化全部后面要用到的变量
        
        代码时许不对

        运算符的优先级不对,在对于结构体 ->优先级过高
        在if后面空格

        每次的读取一行txt文本文件都需要进行释放
        scanf eof
    
    锁的嵌套问题：可重入锁


软件安装：
    版本不匹配

编程规范：
    1，不要过度封装，解决方法，先在原函数中进行编写，如果过大就进行封装，不要提前封装


数组的下标和计数使用：
    数组的计数和下标之间可以互相的转换，
    最好在实参中使用对应的名称 ，而传入的内容进行转换



调试：
    不要不相信调试概率小的内容
    不要牛角尖，换一种简单的写法，重写内容，删除函数