C语言技巧
    switch 
        1,使用gnu的switch规范可以让他拥有更多的数量范围 0 ... 200
    scanf
        1，通过空格 可以一次性的输入多个字符，提高做作业的速度
        不能用
    
    数据类型转换
        使用强制转换,(float)num/n

    函数：
        参数：首地址+数据类型的个数
        拷贝函数头然后到位置后面进行声明，再调用前进行声明：

    变量：
        1，封装成一个结构体，自己使用对应的内容，剩下的给内核使用
    


    利用编译：
        1，利用编译的结果找，没有引用的头文件

        
        f (10 == n) 缺少=号的时候编译报错，你就能够发现了

