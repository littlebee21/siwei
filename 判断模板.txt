判断
    判断的内容的多种写法：
        1，函数式编程
            判断内部
                查询
                    strUtil.is判断
        
    判断的使用技巧
        1，逻辑判断的优先级
            1，参数校验
                逻辑优先级和逻辑运算的整合
            2，判断退出的在前
                判断进入的在后
            3，被依赖的在前
            4，最有可能获取结果的在前
            5，不同状态相同操作的放在一起
            6，先判断状态再判断数据
        
        2，枚举所有可能性
            包含逻辑运算符的如何枚举
            枚举所有条件分治
            自动补全状态划分

        复杂判断
            1,语义化+逻辑运算符的
            2,条件转换:转化为数学条件
                表达式判断
                函数表达式
            3,递归思想
            4,极端假设开始和结束
                状态的开始和结束状态相同
                    状态容器变量判空
                    状态工具变量判空
                while会执行到最后一次成立
        3，使用if noreturn 替代if yesdo
        4, 使用短路运算符 替代 ifif
总结  
    判断的选择
        短路运算符还是嵌套逻辑？
            1，短路运算符适合对于校验成功后的指令执行
            2，嵌套逻辑适合多种条件下下都有指令执行的情况