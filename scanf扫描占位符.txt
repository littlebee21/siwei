scanf扫描占位符
    读入字符串
        %s字符串 整个输入作为一个串,并且设置末尾为 '\0'
        %ns n为整数 读入的串最长不超过n,然后再末尾补 '\0'
        %n[a-z] 读入最多n个字符,如果遇到非a-z的字符,停止
        #[^=]  读入任意多个的字符,直到遇见 "=" 停止
        %n[^=] 读入"="号前的最多n个字符
        %n[^\n] 读入"\n" 前最多n个字符
            