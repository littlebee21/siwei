编程逻辑优化

代码的可读性：
    1，要使用最简单的暴力直观的方式
        增强可读性，直观的符合人的
    2，在拼装函数中使用简单的参数命名；
    3，

函数：
    函数的返回值：
        不一定就只能返回规定的类型，可以使用强转的方式，转过去再转回来
    函数逻辑：
        1，用户输入的参数可以经过判断之后进行纠错
    函数功能分解：
        功能的划分：套索功能，
        环节的划分：
        2，五行以内就没有必要封装成函数
    功能单一化：


格式优化
    1，不要在程序中使用中文，中文编码的方式的多样化会让程序在打开的乱码
    2，在没有形成函数的过程中

    2，对于用户输入的函数要

    争取将循环和判断保持在一层嵌套：
        优化判断：
            使用短路运算符和：
            优先级 if(no)return
            利用if 判断的时序优先级

    提取公共函数
        如果按照我的那种分情况进行  全部重写的代码方式，其实可以编写为一个函数
        类似于将函数进行了进一步的拆分，也就是if break的写法
        是经验不足的写法，


就近：
    1，在使用的时候定义变量，就近定义

健壮性：
    1，校验语句和实际操作语句一半一半
        通过输出处理，然后进行定位改正，
        不要使用printf打印的方式进行问题的定位；
        思想：打印错的，而不要打印对的

