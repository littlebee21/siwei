1. 复习
	[1] GCC 编译流程分析
		1)预处理：删除注释、执行预处理指令
			-E
		2)编译: 将C语言代码转换成汇编代码 
			-S
		3)汇编：将汇编代码转换成二进制代码并生成目标文件
			-c
		4)链接: 将多个目标目标文件和程序库进行链接生成可执行文件
	[2] 注释 
		//行注释 
		/*
			块注释
		*/
	[3] 编码规范
		Linux kernel coding style 	
		
	[4] 基础知识 
		(1) 进位计数制
			1) 基本概念:凡是以进位进行计数的数制称为进位计数制，简称进制
			2) 二进制、八进制、十六进制
		(2) 数制转换
			1) 二进制、八进制、十六进制 ... 转换成 十进制  
				按权相加
					整数部分: 从右向左,每位的位权 N^(i-1)
					小数部分: 从左向右,每位的位权 N^(-j)
			2) 十进制 转换 二进制、八进制、十六进制 ...
				整数部分: 除N求余,逆序排序 
				小数部分: 乘N取整,顺序排序
			3) 二进制 与 八进制 转换  
				从低位到高位,每3位二进制数可以转换成1位八进制数,高位不足补0.
				从低位到高位,每1位八进制数可以转换成3位二进制数
			4) 二进制 与 十六进制 转换  
				从低位到高位,每4位二进制数可以转换成1位十六进制数,高位不足补0.
				从低位到高位,每1位十六进制数可以转换成4位二进制数	
		(3) 整数的编码方式 
			1) 原码 
				最高位为符号位 : 0 表示是正数, 1 表示为负数
				剩余为为数值位 : 存该数的绝对值
			2) 反码 
				最高位为符号位 : 0 表示是正数, 1 表示为负数
				对于正数 反码等于原码 
				对于负数 符号位不变，其余位全部取反
			3) 补码
				最高位为符号位 : 0 表示是正数, 1 表示为负数
				对于正数 补码等于原码 
				对于负数 符号位不变，其余位全部取反, 再加1
			4) 求补运算 
				所有位全部取反，再加1
			5) 计算机使用的是补码存储有符号整数

	[5] 标识符与关键字
		(1) 字符集 
		(2) 标识符  
			标识符：用于标识变量名、符号常量名、函数名、类型名、文件名等的有效字符序列；
			标识符的命名规则
				①　	标识符只能由字母、数字和下划线三种字符组成，且第一个字符必须为字母或下划线；
				②　	C 语言中的标识符大小写敏感；
				③　	用户自定义的标识符不能与关键字同名；
		(3) 关键字 
			关键字：对编译器具有特定含义的标识符，是标识符的一个特殊的集合。C语言内所有的关键字都是小写。
	[6] 变量 
		(1) 什么是变量
			所谓变量是指在程序运行过程中其值可以改变的量。使用变量时必须“先定义，后使用”。
		(2) 定义变量  
			数据类型 变量名;
			eg:  
				int num;
				float 单精度浮点型
				char 字符型
		(3) 变量的实质  
			内存中的一段存储空间，变量名就是这个空间的名字，用户可以通过变量名访问该空间。
		(4) 变量的赋值与初始化  
			运算符 "="
			1) 定义的同时初始化  
				int num = 15;
			2) 先定义变量，后初始化  
				int num; 
				num = 56;
		(5) 变量的地址  
			地址(内存): 计算机内存中每个字节的空间都一个编号，这个编号称为地址
			变量的地址：变量在内存中总占用几个连续的字节，开始字节的地址，就是变量的地址。
			
			运算符 "&"  取地址符
			&变量名;

	[7]	标准输入输出
		(1) 输出 printf 
			#include <stdio.h>							//使用prinf函数需要包含stdio.h			

			int printf(const char *format, ...);
			函数参数: 
				format : 格式控制字符串(用于指定输出数据格式)，包含两种信息
					a.普通字符：原样输出
					b.格式说明符：%[修饰符]格式字符，用于指定输出格式(格式说明符会被替换成 不定参数所指定的值 )
								%d 		表示该位置的数据是个整型数据
								%f		表示该位置的数据是个浮点型数据
								%c		表示该位置的数据是个字符型数据
					  ... : 不定参数，要输出的数据（可以没有，多个时以“,”分隔, 与格式说明符一一对应）
				示例:  
					int a, b;
					a = 15; 
					b = 20;
					printf ("a = %d, b = %d\n", a, b);
			
		(2) 输入 scanf 
			#include <stdio.h>

			int scanf(const char *format, ...);
			函数参数: 
				format : 格式控制串(用于指定输入数据格式),包含两种信息
					a.普通字符 ：原样输入 
					b.格式说明符：%[修饰符]格式字符，用于指定输入数据格式(输入的数据将会被保存到不定参数所指定的变量中)
						%d 		表示该位置的数据是个整型数据
						%f
						%c
				… : 不定参数，输入变量的地址表（多个时以“,”分隔）		
				
				示例:  
					int a, b;		
					
					scanf ("a=%d,b=%d", &a, &b);
					scanf ("%d,%d", &a, &b);
				注意: 格式说明符之间不指定分隔符，默认分隔符 "space", "Enter", "Tab"
					scanf ("%d%d", &a, &b);
					
	[8] 作业  
		作业1：进制转化
				十六进制:4cfd81 转化为八进制
						010 011 001 111 110 110 000 001
						23176601
				十六进制:4003f  转化为二进制
						0100 0000 0000 0011 1111
				八进制:	61345   转化为二进制
						110 001 011 100 101
				十进制:	37.125    转化为二进制
					100101.001
		作业2：交换杯子里的液体
				有两个杯子（用int型代替），其中一个装酒，其中一个装水。如何让两个杯子中的液体交换？
					用 1 表示酒
					用 2 表示水
		#include <stdio.h>

		int main(int argc, const char *argv[])
		{
			int a, b;
			int temp;	//空杯子

			a = 1;	//酒 
			b = 2;	//水

			temp = a;
			a = b;
			b = temp;

			printf("a = %d, b = %d\n", a, b);
			
			return 0;
		}

2. 常量
	[1] 什么是常量
		在程序执行过程中，其值不被改变的量。常量主要分为两类：
		(1) 直接常量：直接引用的数字等；
			字符常量	'A' , 'a'
			整型常量	10 100 0b1101  0664 0xaf
			实型常量	3.14 5.69
			字符串常量 	"hello world" "100"
		(2)符号常量：使用标识符来代替一个数值（常见的：宏定义常量 和 常变量）
		
	[2] 宏定义
		#define 也是C语言预处理命令的一种，被称为宏定义指令。
		所谓宏定义，就是用一个标识符来表示一个文本，如果在后面的代码中出现了该标识符，那么就全部替换成指定的文本。
			
		宏定义基本结构: 
			#define 	宏		替换体
				|		|			|
				|		|		一旦预处理器在程序中找到宏的实例后，就会用替换体代替该宏
				|	一个标识符(一般都会大写)，在代码中会被替换成指定的值
			宏定义指令 
		
		从宏变成最终替换文本的过程称为宏展开(macro expansion)。
		注意：宏定结束后不要添加分号;
		(1) 类对象宏(object-like macro 不带参宏) 
			#define 宏名 宏值
			例如：
				#define MAX 10
				#define PI 3.1415926
		(2) 类函数宏(function-like macro 带参数宏)
			#define 宏名(参数列表) 宏值
			例如：
			#define S(r) PI*r*r
			示例: 使用宏定义PI来定义3.1415926，定义宏定义，计算圆的面积			
				#include <stdio.h>

				#define PI 3.1415926
				#define S(r) PI * r * r 

				int main(int argc, const char *argv[])
				{
					int r;
					float s;

					printf("please input r :\n");
					scanf("%d", &r);

					s = S(r);

					printf("s = %f\n", s);
					
					
					return 0;
				}

		练习:租船问题。
			写程序计算游客需要付的租船的费用。租船每小时30元，押金100元。
			游客输入租船时间，计算出租船费用。租船费用=时间*每小时钱数+押金。
			要求押金与每小时钱数使用宏定义，租船费用公式用宏定义表示。
			
			#include <stdio.h>

			#define YAJIN 100
			#define PERHOUR 30
			#define MONEY(t) YAJIN+PERHOUR*t

			int main(int argc, const char *argv[])
			{
				int hour;
				int money;

				printf("please input hour :\n");
				scanf("%d", &hour);

				money = MONEY(hour);

				printf("Money : %d\n", money);
				
				return 0;
			}

		注意:　宏定义仅仅只是简单的替换，使用足够多的圆括号来确保运算和结合的正确顺序
			
	[3] 常变量 
		常变量(只读变量)：变量值不可改变的量，使用const修饰
		定义变量:
			const 数据类型 变量名 = 初值;
		示例: 
			const int a = 10;	//定义一个常变量
			a = 9;				//报错 ❌
		注意：
			const修饰后的变量会变成只读，因此无法再次赋值。因此初始化常变量需要在定义时直接赋值。
		作业: 
			将求圆面积的例子改成const.
			
3. 数据类型 
	[1] 基本数据类型：C语言中已经定义的一种类型，基本数据类型最主要的特点是，其值不可以再分解为其它类型。
		整型 int
		字符型 char
		浮点型：
			单精度浮点型 float
			双精度浮点型 double
	[2] 构造数据类型：是根据已定义的一个或多个数据类型用构造的方法来定义的。也就是说，一个构造类型的值可以分解成若干个“成员”或“元素”。
		数组 
		结构体 struct 
		联合体/共用体 union 
		枚举型 enum 
	[3] 指针类型 : 指针是一种特殊的，同时又是具有重要作用的数据类型。其值用来表示某个量在内存储器中的地址。
	[4] 空类型: 表示什么都不是，常用于函数返回值或者定义空指针。
			void 
			
4. 整型 
	[1] 整型的类型
		基本型：类型说明符为int：值必须是整数，可以是正的，负的或者是0；其取值范围依赖于计算机系统。
		短整型：类型说明符为short int 或 short，用于仅需小数值的场合以节省空间。
		长整型：类型说明符为long int 或 long，用于使用大数值的场合。
		
		有符号整型: 类型说明符 signed 
		无符号整型: 类型说明符 unsigned 
		注意:如果省略 类型说明符默认类型为 signed 类型  
		
	[2] 整型长度（32bit机）
		实际情况也确实如此， C 语言并没有严格规定 short、 int、 long 的长度，只做了宽泛的限制：
			short 至少占用 2 个字节。
			int 建议为一个机器字长。 32 位环境下机器字长为 4 字节， 64 位环境下机器字长为 8 字节。
			short 的长度不能大于 int， long 的长度不能小于 int。
		总结起来，它们的长度（所占字节数）关系为：
		2 ≤ short ≤ int ≤ long
		
		
		类型				占用内存字节数			数的范围
		signed int				4个字节				-2^31 ～ 2^31-1
		signed short			2个字节				-2^15 ～ 2^15-1
		signed long				4个字节				-2^31 ～ 2^31-1
		unsigned int			4个字节				0 ～（2^32-1）
		unsigned short			2个字节				0 ～ (2^16-1)
		unsigned long			4个字节				0 ～（2^32-1）
			
		
	[3] sizeof运算符
		获取某个数据类型或变量的长度可以使用 sizeof 操作符，以字节为单位来计数。
		语法格式: 
			sizeof(数据类型/变量)
					
		注意: 对于sizeof(),其形式类似于函数，但其本质是C语言的一个运算符，并且对 sizeof() 的处理都是在编译阶段进行。
		
		#include <stdio.h>

		int main(int argc, const char *argv[])
		{
			printf("The size of int is %d\n", sizeof(int));
			printf("The size of short int is %d\n", sizeof(short int));
			printf("The size of long int is %d\n", sizeof(long int));
			printf("The size of unsigned int is %d\n", sizeof(unsigned int));
			printf("The size of unsigned short int is %d\n", sizeof(unsigned short int));
			printf("The size of unsigned long int is %d\n", sizeof(unsigned long int));
			
			return 0;
		}

	[4] 整数型常量
		在C语言中，整常数有二进制、八进制、十六进制和十进制四种。在程序中是根据前缀来区分各种进制数的
		(1) 十进制
			十进制整常数没有前缀。其数码为0～9，只要常数中不含小数点和指数的数编译器都会当作整数。
			合法的表达方式：237、-568、65535、1627；
			非法的表达方式：023 (不能有前导0)、23D (含有非十进制数码)。
		(2) 八进制
			八进制整常数必须以0开头，即以0作为八进制数的前缀。数码取值为0～7。
			合法的表达方式：015(十进制为13)、0101(十进制为65)、0177777(十进制为65535)；
			非法的表达方式：256(无前缀0)、03A2(包含了非八进制数码)。
		(3) 十六进制：
			十六进制整常数的前缀为0X或0x。其数码取值为0~9，A~F或a~f。
			合法的表达方式：0X2A(十进制为42)、0XA0 (十进制为160)、0XFFFF (十进制为65535)
			非法的表达方式：5A (无前缀0X)、0X3H (含有非十六进制数码)。
		(4) 二进制
			二进制由 0 和 1 两个数字组成，使用时必须以 0b 或 0B（不区分大小写）开头
			合法的表达方式：0b101(换算成十进制5)、0b110010（换算成十进制50）、0B100001（换算成）；
			非法的表达方式：101010（无前缀0B，相当于十进制）0B410（含有非二进制数码）。

		注意，标准的 C 语言并不支持上面的二进制写法，只是有些编译器自己进行了扩展，才支持二进制数字。
	
	[5] 整数的显示/打印方法
		(1) 显示十进制整数
			%hd、 %d、 %ld 以十进制、有符号的形式输出 short、 int、 long 类型的整数
			%hu、 %u、 %lu 以十进制、无符号的形式输出 short、 int、 long 类型的整数
		(2) 显示八进制整数
			%ho、 %o、 %lo 以八进制、不带前缀、无符号的形式输出 short、 int、 long 类型的整数
			%#ho、 %#o、 %#lo 以八进制、带前缀、无符号的形式输出 short、 int、 long 类型的整
		(3) 显示十六进制整数
				以十六进制、不带前缀、无符号的形式输出 short、 int、 long 类型的整数。如果 x 小写，那
			么输出的十六进制数字也小写；如果 X 大写，那么输出的十六进制数字也大写。
			%hx、 %x、 %lx
			%hX、 %X、 %lX
				以十六进制、带前缀、无符号的形式输出 short、 int、 long 类型的整数。如果 x 小写，那么
			输出的十六进制数字和前缀都小写；如果 X 大写，那么输出的十六进制数字和前缀都大写。
			%#hx、 %#x、 %#lx
			%#hX、 %#X、 %#lX
		注意
			1）虽然部分编译器支持二进制数字的表示，但是却不能使用 printf 函数输出二进制，这一点比较遗憾。
			2）按八进制或者十六进制打印一个signed整数没有意义, 其结果会将signed数转化为unsigned数打印。
			3）显示值的类型要同说明符相匹配；当格式控制符和数据类型不匹配时，编译器会给出警告，提示程序员可能会存在风险。

		#include <stdio.h>

		int main(int argc, const char *argv[])
		{
			short a = 0b01111111;
			int b = 0177;
			long c = 0x7F;

			printf("a = %#ho,b = %#o,c = %#lo\n", a, b, c);
			printf("a = %hd,b = %d,c = %ld\n", a, b, c); 
			printf("a = %#hx,b = %#x,c = %#lx\n", a, b, c);
			printf("a = %#hX,b = %#X,c = %#lX\n", a, b, c);
			
			return 0;
		}
		
	[6] 整型的溢出
		整数溢出程序不会报错，所以写程序时一定要小心，当达到最大值时会重新从起点开始。
		
		#include <stdio.h>
		#include <limits.h>

		int main(int argc, const char *argv[])
		{
			int max = INT_MAX;
			int min = INT_MIN;
			unsigned int umax = UINT_MAX;

			printf("max = %d\n", max);
			printf("min = %d\n", min);
			printf("umax = %u\n", umax);
			printf("max + 1 = %d\n", max + 1);
			printf("min - 1 = %d\n", min - 1);
			printf("umax + 1 = %u\n", umax + 1);
			
			return 0;
		}

5. 浮点型(实型)
	[1] 浮点型的种类  
		单精度（float型）				4byte
		双精度（double型）				8byte
		长双精度型（long double）		12byte
		
	[2] 浮点数的表示形式
		(1)小数形式：由数码0~ 9和小数点组成。例如 0.0、 75.0、 4.023、 0.27、 -937.198 等都是合法的小数
		(2)指数形式：例如 7.25×(10^2)、 0.0368×(10^5)、 100.22×(10^-2)、 -27.36×(10^-3) 等。任何小数都可以用指数形式来表示。
			C 语言中小数的指数形式为：
				aen	或 aEn 
			说明： 
				a 为尾数部分，是一个十进制数；
				n 为指数部分，是一个十进制整数；
				E 或 e 是固定的字符，用于分割尾数部分和指数部分。整个表达式等价于 a×(10^n)
			示例: 
				2.1E5 = 2.1×(10^5)，其中 2.1 是尾数， 5 是指数。
				3.7E-2 = 3.7×(10^-2)其中 3.7 是尾数， -2 是指数。
				0.5E7 = 0.5×(10^7)，其中 0.5 是尾数， 7 是指数。

	[3] 数据的输出格式 
		小数也可以使用 printf 函数输出，包括十进制形式和指数形式，它们对应的格式控制符分别是：
			%f:输出十进制计数法的float和double数字；
			%e：输出指数计数法的float和double数字；
			%lf、%le：输出double类型的数字；
			%Lf、%Le：输出long double类型的数字；
			
		#include <stdio.h>

		int main(int argc, const char *argv[])
		{
			float a = 0.302;
			double b = 123.0;
			long double c = 0.7623e-2;

			printf("a=%e a=%f\n"
					"b=%e b=%f b=%le b=%lf\n"
					"c=%Le c=%Lf\n",a, a, b, b, b, b, c, c);
			
			return 0;

		}
		
		练习：一个水分子的质量约为3.0*10^(-23)g,1夸脱水大约950g,编写程序，要求输入水的夸脱数，然后显示这么多水中包含多少水分子？
		#include <stdio.h>

		#define M 3.0e-23
		#define K 950

		int main(int argc, const char *argv[])
		{
			int kuatuo;

			printf("请输入夸脱数 : \n");
			scanf("%d", &kuatuo);

			printf("水分子数 = %e\n", K / M * kuatuo);
			
			return 0;
		}
		
		
	[4] 浮点数在内存的存储方式
		(1) 国际标准IEEE 754
			V =  (-1)^S * M * 2^E 
			
			说明： 
				V 表示二进制浮点数 
				S 表示符号位 	当 S=0 时 V 为正数,当 S=1 时 V 为负数 
				M 表示有效数字	大于等于1,小于2
				E 表示指数位 
			例如: 
				以 2.5 为例：10.1 (二进制)
				
				2.5 = 1*(2^1)+0*(2^0)+1*(2^-1) = 10.1 = 1.01 * 2^1,
				
				所以 S = 0, M=1.01, E=1	
				
		(2) 单精度float的存储方式
			V =  (-1)^S * 1.f * 2^E 
			
			|s|		|e|		|f|
			31		30:23	22:0
			最高的1位是符号位s
			接着的8位用来表示指数E
			剩下的23位用来表示标准有效数字M的小数部分。
		示例: 
			单精度 50.0 = 0b110010.0 = -1^0 *　1.100100 * 2^5 
				s = 0 
				e = 5 
				f = 0.1001000...  
				
			|0| |e+127| |1001000...|
			
			 0 10000100 10010000000000000000000
			
			
		FQA:
			Q1：为什么有效数字只保留小数部分？
			A1：是因为整数部分是肯定为1，所以直接被省略了，取数时候会自动加上1来计算
			
			Q2：上面的例子中，我们知道E代表的是幂的大小，而存入计算机的e则为E+127，那么问题来了，这里为什么要加上127这个数呢？
			A2：计算机表示单精度浮点数时，是用8位去存储指数部分，在数值上面，表示0~255，但是我们同样需要有负指数，正负指数的位数量为了均等，各自一半，-127~128。储存时候会加上127，这样就刚刚好是0~255，就能很好的储存了，不然的话，需要判断符号位来判断数值的正负。

			Q3：无符号的浮点数怎么存储？
			A3：遗憾的告诉你，根据IEEE 754，浮点数必须是有符号的，不可以使用(usigned 来声明浮点数)。
			
		(3) 双精度double的存储方式
			Double型（双精度），64位。 
			最高位为符号位	s 
			接着11位表示指数 E
			剩下52位表示有效数字M的小数部分f。
			
			double的指数(E)是11位，所以如果是double，指数值需要与1023（2^(11-1) – 1）相加
					
			作业：求双精度浮点数123.125(十进制)在计算机中的表示，先根据公式计算再用GDB工具检验
	
	[5]	不同类型浮点数的取值范围
		类型		位数		有效位数		取值范围
		float		32位		6~7位			10^-38 ~ 10^38           
		double		64位		15~16位			10^-308~ 10^308
		long double	96位		18~19位			10^-4932~10^4932
		
	[6] 浮点数的溢出
		对于浮点数，有上溢和下溢两种情况。试图存储绝对值很小的数将导致下溢的情况，而试图存储绝对值很大的数将导致上溢的情况。
			
			
				
6. gdb 软件调试工具  
		(1) gcc 编译时需要添加  -g  添加调试信息
		(2) 打开gdb  
			gdb 可执行程序名 
		(3) gdb 指令  
			l 		显示代码  
			b num	添加断点	
			p 		查询变量的值或地址
			r		运行程序
			n 		单步运行  
			c 		恢复运行
			x		查看内存中的数据  
			q 		退出
			
			x/<n/f/u> <addr>
				n、f、u是可选的参数。
			n是一个正整数，表示需要显示的内存单元的个数，也就是说从当前地址向后显示几个内存单元的内容，一个内存单元的大小由后面的u定义。
			f 表示显示的格式，参见下面。如果地址所指的是字符串，那么格式可以是s，如果地十是指令地址，那么格式可以是i。
				x 按十六进制格式显示变量。
				d 按十进制格式显示变量。
				u 按十六进制格式显示无符号整型。
				o 按八进制格式显示变量。
				t 按二进制格式显示变量。
				a 按十六进制格式显示变量。
				c 按字符格式显示变量。
				f 按浮点数格式显示变量。
			u 表示从当前地址往后请求的字节数，如果不指定的话，GDB默认是4个bytes。u参数可以用下面的字符来代替，
				b表示单字节，
				h表示双字节
				w表示四字节
				g表示八字节。
			<addr>表示一个内存地址。
				
			例如:命令：x/3uh 0x54320 表示
				从内存地址0x54320读取内容，h表示以双字节为一个单位，3表示输出三个单位，u表示按十六进制显示。

			练习：求单精度浮点数123.125(十进制)在计算机中的表示
				先根据公式计算再用GDB工具检验
				
				123.125 = 111 1011.001 = 1.111011001 * 2^6; 
				s = 0 
				e = 6 
				f = 111011001
				
				0 	10000101 11101100100000000000000
				0	10000101 11101100100000000000000
				
	作业:
		1)复习今天上课的内容，整理今天的笔记(晚自习结束前提交)
		2)准备早分享(分享昨天学习的内容、其他知识、分享个人经验...)	
		3)学习计划
			
		

		
			
		
