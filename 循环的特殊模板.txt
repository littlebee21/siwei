循环的特殊模板
    1，循环嵌套
        循环嵌套的意义
            1，将多篇内存空间降维为少量内存空间；最多三个进行操作（链表）
            2，内层循环函数想要使用有规律的外层变量（作用域原理）
                1,外层循环的数据可以被用到内层循环,例如n,而且经常
                eg：三角形打印
                eg：冒泡
                    内存外层
                    for(i=0;i<n;i++){ 参数是i ，内部只需要关注i，想象将i圈起来，for为形容词，此函数没有名字，所以称作为循环的查看或者
                                                //写上一个空行，表示循环的获取了某一个参数，但是是隐形的，在头里做了初始化
                        for(j=0;j<)
                    }

        循环嵌套原理
            循环盒子模型jpg ！！！！化繁为简
            循环雨伞模型jpg！！！！！！
                当进入循环内，就相当于定义了一个固定变量
                当跳出循环外，这个变量就会失效，
                其他的内容当作单层嵌套的结构语句进行编写

            思考模型：
                循环的参数：原本是变化的，但是进入到函数体里，就变成静态的
                何时变为动态？当循环再次回到函数头，循环头

        
    
    
    
    
    2，一个循环，多个指针
        双指针甚至多指针问题；
        1，指针位置的改变（i++）分布分散，但是一定分布在记录数据的内容之后：
        eg：对于字符串的拷贝：
            int i;
            int j;
            for(;;i++){
                j++;
            }



