C语言规范 - 变量的使用
    1.变量的声明定义
        1.修饰符使用
            static的作用
            关键字const
            关键字volatile防止优化
            关键字register寄存器
            访问固定内存位置--强制类型转换
            typedef类型
        2.指针初始化
            指向数据类型
            指针名字*p
                取指针指向符号
                指针变量
        2.函数的定义
            输入参数
                值传递
                地址传递
                    作用
                连续空间的传递
                    字符空间
                        strlen
                        strcpy
                    非字符空间
                        memcpy
                        void *
            返回值
                返回基本数据类型
                返回连续空间类型
                    函数内部实现
                        1、静态区
                        2、只读区
                        3、堆区
                返回地址
            指针保存函数
                int (*fun)()  指向函数首地址
                int *fun(）返回值为int 类型
        2.数组空间初始化
            初始化语法
                初始化修饰符
                    unsigned char buf[10];----> data
                        数据采集
                    char buf[10]; ----> string
                数组名
                数组运算符
                空间
                    字符空间
                    非字符空间
            非字符空间初始化
                a[0] = 10逐次初始化变量
                int a[10] = {10,20,30};一次初始化
            字符空间初始化
                字符数组初始化char buf[5]={'h','e','l','l','o'};
                1.字符串常量初始化char p2[] = "Another Hello";
                    char str[];     str="I am happy"
                2.字符串常量初始化char *p1 = "Hello";
            指针保存数组名
        数据声明和数据调用方式不同
    p指针的使用
        用指针代替数组
    array数组的使用
        数组的拷贝
    主题
        变量类型
            使用无符号整数u8，而不是整数的8
        功能
            单一功能
                1、一个变量只有一个功能，不能把一个变量用作多种用途
                4、防止局部变量与全局变量同名
            描述对象
                2、结构功能单一，不要设计面面俱到的数据结构
        异常
            初始化
                6、严禁使用未经初始化的变量作为右值
            类型转换
                10、尽量减少没有必要的数据类型默认转换与强制转换
        线程安全
            3、不用或者少用全局变量
                全局变量和函数传递参数的区别
            变量引用
                [1]不提倡用全局变量，不用extern int value; 这类声明；
                1.h头文件中声明extern int value；在2.h里面包含1.h用来调用value。
            封装控制修改
                7、构造仅有一个模块或函数可以修改、创建，
                访问的全局变量，防止多个不同模块或函数都可以修改、创建同一全局变量的现象
            cashion
                9、明确全局变量的初始化顺序，避免跨模块的初始化依赖
        解耦
            8、使用面向接口编程思想，通过 API 访问数据：如果本模块的数据需要对外部模块开放 ，应提供接口函数来设置、获取，同时注意全局数据的访问互斥
        序列化
            顺序
                5、通讯过程中使用的结构，必须注意字节序