java面向对象
    语法体现的类思想
        封装
            get和set
        继承
            概念
                3.1、继承的描述
                    单继承
                        一个类，只能继承一个父类
                        一个类，可以有多个子类
                    不继承
                        构造方法不继承
                        没有访问权限的成员不继承
                        静态成员不继承
        多态
            子类转父类规则
                 将一个父类的引用指向一个子类的对象,称为向上转型(upcastiog),自动进行类型转换.
                此时通过父类引用调用的方法是子类覆盖或继承父类的方法,不是父类的方法.
                 此时通过父类引用变量无法调用子类特有的方法
                如果父类要调用子类的特有方法就得将一个指向子类对象的父类引用赋给一个子类的引用,称为向下转型,此时必须进行强制类型转换
    类的原理
        1.7、对象的初始化顺序
        所有的Java 程序由public static void main(String []args)方法开始执行。
    抽象类和接口
        6.1、抽象类
        6.2、接口
            作用
                结构设计工具，用来解耦合
                用来抽象出类的方法
            用interface替代class
            用implements替代extends
            接口中只能定义
                公共的常量
                公共的抽象方法
                公共的内部类、内部接口
                JDk1.8之后可以有默认的实现和常量
        6.3、多继承的分析
    类的框架组成
        Java程序基本结构：class
        4、类的访问控制
            耦合
                4.2、default 包内可问
                4.3、protected 子类访问
                    父类中声明为 protected 的方法在子类中要么声明为 protected，要么声明为 public，不能声明为 private。
                4.4、public都可访问
                    父类中声明为 public 的方法在子类中也必须为 public。
            解耦合
                4.1、private 类内访问
                    父类中声明为 private 的方法，不能够被继承。
                4.5、get/set+private
            2、static关键字类变量
                1.9、Static初始化顺序
            接口里面都是public
        类的五大成分
            构造函数
            public method/prop属性
            内部类
                static class静态内部类
                    使用static修饰的成员内部类，称之为静态内部类
                    静态内部类只能访问外部类的静态成员
                    不能直接访问外部类的非静态成员
                class非静态内部类
                    非静态内部类依赖于外部类对象存在
                    在非静态内部类中，不能定义静态成员
                局部内部类
                    定义
                        声明在方法内部的类
                        局部类型，只能在局部使用
                        但它的实例，可以转为父类型传递出去
                    注意
                        不可以使用任何访问权限修饰符
                        不可以直接访问局部变量（方法的参数等效于局部变量）
                        如果一定需要访问，则需要使用final对局部变量进行修饰
                成员内部类
                    在类（外部类）的内部，且与外部类的成员是“同一级别”的
                匿名内部类
                    直接创建已知的类的子类的对象，则该对象的类型就是匿名内部类
            内部属性
                对象属性初始化
                静态属性和动态属性
            内部方法
                setter getter存取
                方法 参数 函数重载
    类的使用
        实现面向对象的三大特征
            401.java封装的实现
            401.java继承的实现
                1.实现一个接口public static LaoShi implement teach(static fenbi)
                2、继承类的初始化
            401.java多态的实现
                1.继承的存在
                2.方法重写
                    3.2、方法的重新
                    @Override
                        equals()
                            Java中的equals()方法
                父类引用变量指向子类对象(子类到父类的类型转换)
        访问方法
            super
                1.访问父类
                    访问父类方法/属性
                    继承父类的属性初始化
            内部类访问
            内部属性访问
            this指针
                作为方法
                    this（）调用当前类的构造函数
                    - this用在方法中，谁调用这个方法，this就代表谁
                    - this代表了当前对象的引用
                在构造器中
                    - this用在构造器中，代表构造器正在初始化的那个对象的引用
                        使用this关键字来区分局部变量和实例变量
                        不需要 this 关键字的程序示例
                作为参数传递
                    this：作为参数传递给方法
                    this：在构造函数调用中作为参数传递
                作为返回
                    this关键字用来返回当前类的实例
            5.获取类的实例
                使用java反射
        面向对象属性的初始化
    主题
        抽象类
        相对具体的类
        接口