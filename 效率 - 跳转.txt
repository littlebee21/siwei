效率 - 跳转

3，文件结构标明
4，跳转空行
5，跳转函数头
6，多行光标跳转

策略：
    最快：ide：快捷键跳转
    常用：标记
        太慢：确定标记
            0，使用查看目录的方式确定对应：函数名，类型名，变量名
            0，头文件方式
            0，toptree方式
        设计标记
            1，使用检索的方式检索到对应位置： 函数名
            2，自定义对应的标记
                1，todo标记：
                2，did标记：表示经过需改，时间戳+人名
                3，var，function，declare 元素类型标记
        创建标记：
            使用时间戳的+脚本的 alt+' //gao2022-0503-17创建对应标记位
        跳转标记：
            alt+f搜索不要加粘贴，不会排序，按照常规顺序
        删除标记：
            使用：1,每天工作结束之后进行替换
            放弃，search and replace进行删除：每次写完替换太慢

    最慢：搜索字符还要进行遍历查重
        2，不进行输入的查找： 双击ctrl+f
