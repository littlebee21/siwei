搜索 - linux
    查找文件
        find
        locate
            updatedb更新查找数据库
        whereis
            查询位置
        which
            找到PATH变量中的路径下的可执行文件
        type查找bash内置命令
        locate
            查找符合要求的文档
    终端文本查找
        man手册
        grep进行字符过滤
            通配符
            管道
        ctrl+shift+find
            不好用
    查找并替换
        s在扩展命令模式下使用为替换要使用地址定界开始查找格式:s/要查找的内容/要替换的内容/装饰器
        要查找的内容可以使用正则
        替换的内容不能为正则
            可以为分组:%s(nfs.*)/#\1/\1表示第一个分组内的内容&amp;可以代表前面匹配的内容
        装饰器
            i 忽略大小写
            g 表示全部替换
            gc 每次替换之前都要询问是否替换
        分隔符还可以使用
            @
            #
        s在扩展命令模式下使用为替换要使用地址定界开始查找格式:s/要查找的内容/要替换的内容/装饰器
        要查找的内容可以使用正则

    linux下的软件
        locate 定位 ：低版本的ubuntu无法似乎用更为复杂的软件，12.04
             + updatedb 更新数据库发
             + head ：剪裁 少量的内容
             + vim  : 打开
             + 鼠标 ：当输出的命令特别多就需要二次过滤和，或者直接在终端使用右键进行复制
        fd 命令
        fzf
            模糊搜索工具
                类似everthing
        neovim 更高级的vim
        终端设置成vim，emac模式