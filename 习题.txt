1. 编写一个程序实现功能，字符串翻转，用指针实现。


2. 编写一个程序实现功能：将两个字符串合并为一个字符串并且输出，用指针实现。
   char str1[20]={“Hello ”}, str2[20]={“World ”};
   
3. 解说下列程序并输出结果
	#include<stdio.h>
	
	int main()
	{
		int m =100;    //？
		int **q;       //？
		int *p;        //？
		p = &m;        //？
		q = &p;        //？
		
		printf("m=%d, &m=%p, p=%p ,&p=%p\n",m,&m,p,&p);    //？
		printf("q=%p, &q=%p, *q=%p, **q=&d\n",q,&q,*q,**q);    //？
	
		return 0;
	}

5. 数组中存储了若干字符串的地址，则这样的数组就叫作___字符串数组_____。


6. 定义一个字符指针数组 char *p[]={"Beijing" , "New York" , "London" , "Paris" , "Moscow"} 
	并将其打印出来
   要求：（使用指针）
   
   
7. 写出 指针数组和数组指针 的定义和基本形式及调用，并举例

vim 
8. 数组元素的表示有_________和_________两种方式。


9. c语言中 ，数组地址（数组名）与指针变量具有相同的访问效果。
   指针变量是____。数组地址(数组名)是_____，________。
	a.变量   常量   可以进行算术运算（++,--）
	b.常量   变量   不可以进行算术运算（++,--）
	c.变量   常量   不可以进行算术运算（++,--）
	b.常量   变量   可以进行算术运算（++,--）

10. 指针与数组常见操作总结   eg:  int a[]={1,2,3,4,5.....};     int *p = a;    
    问题                          数组表示         指针表示
	数组名是第一个元素的地址         a			 p
	数组的第一个元素				a[0]			*(p+0)
	数组第i个元素的地址 			&a[i]          p+i
	数组第i个元素					a[i]		   *(p+i)
	数组第一个元素加m				a[0]+m			*p + m

11. 定义一个二维数组a，有3行3列,如何表达第二行第二个元素的地址? 写出四种形式
	int a[3][3] = { {1, 3, 5,}, {9, 11, 13}, {17, 19, 21} }

	&a[1][1]
	a[1]+1
	*(a+1)+1
	&a[0][0] + 3*1+1














